[general]
project_type = cpp
project_path = .
verbose_level = Debug
output_path = output

[build]
  [[build_options]]
    CMAKE_BUILD_TYPE = Release
    CMAKE_INSTALL_PREFIX = release/libs

[deploy]
  name_artifact = pdfwriter
  targets = build/release/libs/lib, build/release/libs/include
  [[nexus]]
    auth_required = True
    credentials_env = NEXUS_CREDENTIALS
    url = http://10.10.30.61:9001/nexus/repository/raw-dataset-biometrics
    folder = libs/pdfwriter
